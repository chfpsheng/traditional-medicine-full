{
  "name": "traditional-medicine-full",
  "version": "1.0.0",
  "description": "三端应用：Electron + 小程序 + PC后台管理",
  "private": true,
  "workspaces": [
    "server",
    "electron",
    "miniprogram",
    "admin"
  ],
  "scripts": {
    "dev:server": "npm run dev --workspace=server",
    "dev:electron": "npm run dev --workspace=electron",
    "install:all": "npm install && npm install --workspace=server && npm install --workspace=electron"
  },
  "keywords": [
    "electron",
    "traditional-medicine",
    "api"
  ],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@vitejs/plugin-vue": "^4.4.0",
    "electron": "^27.0.3",
    "vite-plugin-electron": "^0.15.0",
    "vite-plugin-electron-renderer": "^0.14.5"
  }
}
